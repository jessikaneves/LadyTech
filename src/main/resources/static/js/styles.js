/**
* Here are all the JS inits from our Materialize elements
*/

document.addEventListener('DOMContentLoaded', function() {
    var status = document.querySelectorAll('select');
    var instances = M.FormSelect.init(status, {});
});